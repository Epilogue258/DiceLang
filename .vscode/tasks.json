{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "pytest: current file",
            "type": "shell",
            "command": "${command:python.interpreterPath} -m pytest ${file} -v -s",
            "problemMatcher": [],
            "group": {
                "kind": "test",
                "isDefault": true
            }
        }
    ]
}
